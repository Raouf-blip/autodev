name: actions

on:
  push:
    branches: ["feature"]

jobs:
  workflow:
    runs-on: ubuntu-latest
    steps:
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
      - uses: actions/checkout@v4
      - name: echo for test
        run: echo "FICHIER de CI/CD bien executé"